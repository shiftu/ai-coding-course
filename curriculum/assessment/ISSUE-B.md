# Issue #12 · 日报的"最忙时段"是错的，顺便提两个需求

**标签**：bug, enhancement · **优先级**：高

今天早会把值班日报投屏，"最忙时段"那一栏写的是 `2026-08-17`。
底下笑了半天——这是个日期，不是时段。麻烦看一下。

另外既然要动这块，顺便一起做了吧：

1. 加个 `--team <名字>` 参数，只统计某个组。现在交易组和基础架构组的告警全混在一起，
   我只想看自己组的。
2. 输出改成企业微信机器人的卡片格式，能直接推群，省得每天手动复制 Markdown。
   顺便把每个人的响应时间也放进卡片里——老板一直想知道值班期间到底谁扛得最辛苦。

辛苦了。
